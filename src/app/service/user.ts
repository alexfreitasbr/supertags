import { User } from "../model/User"


export const user:User | null = {
    cod_email: "alex@ElementInternals.com",
    nom_user:  "alex",
    des_senha: "alex",
    }

// export const user:User | null = null