import { IconSettings } from "@tabler/icons-react";

export default function Settings() {
    return <div className="flex items-center gap-3 text-3 text-white"><IconSettings size={30}/> Settings</div>
}